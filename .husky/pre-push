if [ "$SKIP_E2E" = "1" ]; then
  echo "ℹ️  E2E tests skipped (SKIP_E2E=1)"
else
  echo "🎭 Running E2E tests before push..."
  npm run test:e2e
  if [ $? -ne 0 ]; then
    echo "❌ E2E tests failed. Push aborted. (use SKIP_E2E=1 to skip)"
    exit 1
  fi
  echo "✅ E2E tests passed."
fi
